server:
  port: 8080

debug: true
# trace: true
logging:
  level:
    root: @log.level.root@

spring:
  output:
    ansi:
      enabled: always
  thymeleaf:
    cache: false

eureka:
  client:
    registerWithEureka: false